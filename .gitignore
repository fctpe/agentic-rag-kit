# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/

# node
node_modules/
.next/
out/

# env & secrets
.env
.env.*
!.env.example

# terraform — provider binaries and state. .terraform.lock.hcl IS committed:
# it is what makes `terraform init` in CI resolve the same provider build.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# data
data/corpus/

# misc
.DS_Store
*.log
.claude/

# scratch from the CI reproducibility step (two ingests + two eval runs, diffed)
backend/digest-*.txt
backend/eval-*.json
