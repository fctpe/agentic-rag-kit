apiVersion: v1
kind: Service
metadata:
  name: ragkit-backend
  labels:
    app.kubernetes.io/name: ragkit
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ragkit
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
