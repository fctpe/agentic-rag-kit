apiVersion: v1
kind: Service
metadata:
  name: ragkit-frontend
  labels:
    app.kubernetes.io/name: ragkit
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ragkit
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
