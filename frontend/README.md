# agentic-rag-kit frontend

Next.js chat UI: streaming answers, the citation panel, and the human-approval banner.
See the repository root README for the full documentation.

```bash
npm ci
npm run dev     # :3000 — needs the backend on :8000 (make api)
npm test        # 17 tests
```
